<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">
        
        <ImageView
            android:layout_width="fill_parent"
            android:layout_height="150dp"
            android:src="@drawable/watch_banner"
            android:id="@+id/list_top_image"
            android:scaleType="fitXY"/>

        <TextView
            android:layout_width="fill_parent"
            android:layout_height="wrap_content"
            android:textSize="30sp"
            android:background="@drawable/title_head_bg"
            android:layout_below="@id/list_top_image"
            android:id="@+id/todays_task_head"
            android:textColor="@color/light_indigo"
            android:padding="5dp"
            android:text="@string/home_head_title"/>
        
        <ListView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:divider="@color/dark_grey"
            android:dividerHeight="3dp"
            android:layout_marginTop="5dp"
            android:layout_marginBottom="2dp"
            android:layout_below="@id/todays_task_head"
            android:id="@+id/home_task_list">

        </ListView>

        <android.support.design.widget.FloatingActionButton
            android:layout_width="70dp"
            android:layout_height="70dp"
            android:src="@drawable/add_icon"
            app:backgroundTint="@color/white"
            android:layout_alignParentRight="true"
            android:layout_alignParentBottom="true"
            android:layout_marginRight="30dp"
            android:layout_marginBottom="70dp"
            android:id="@+id/add_floating_button"/>

    </RelativeLayout>


</android.support.constraint.ConstraintLayout>